# 第二阶段实施计划：AI对话式辅导

> 基于当前代码库现状分析，评测体系（8个API端点 + 完整前端）已全部实现。
> 第二阶段剩余的核心工作是 **AI对话式辅导** 功能的全栈实现。

---

## 一、当前进度总览

| 模块 | 状态 | 完成度 |
|------|------|--------|
| 评测体系（7个评分维度API） | ✅ 已完成 | 100% |
| 评测前端（雷达图+报告页） | ✅ 已完成 | 100% |
| 评测流程（选题→答题→提交→查看） | ✅ 已完成 | 100% |
| 管理后台（评测管理+维度管理） | ✅ 已完成 | 100% |
| 数据库（所有评测相关表） | ✅ 已完成 | 100% |
| AI对话式辅导 | ❌ 未开始 | 0% |

---

## 二、AI对话式辅导 - 实施方案

### 2.1 架构设计

```
┌─────────────────────────────────────────────────┐
│                   前端 (Vue 3)                   │
│  ┌─────────────┐  ┌─────────────┐  ┌──────────┐ │
│  │ ChatView.vue│  │ ReportView  │  │ Advice   │ │
│  │ (对话页面)   │  │ (报告页面)  │  │ View.vue │ │
│  └──────┬──────┘  └──────┬──────┘  └────┬─────┘ │
│         │                │              │        │
│  ┌──────┴────────────────┴──────────────┴─────┐  │
│  │              api/chat.js                   │  │
│  │        (对话API封装 + SSE流式处理)           │  │
│  └────────────────────┬───────────────────────┘  │
└───────────────────────┼──────────────────────────┘
                        │ HTTP/SSE
┌───────────────────────┼──────────────────────────┐
│                后端 (Flask)                        │
│  ┌────────────────────┴───────────────────────┐  │
│  │           scoring_api.py (路由)              │  │
│  │  POST /chat             - 发送消息获取回复    │  │
│  │  GET  /chat/history     - 获取对话历史        │  │
│  │  GET  /chat/session     - 获取当前会话        │  │
│  │  POST /chat/session     - 创建新会话          │  │
│  │  DELETE /chat/session/:id - 删除会话          │  │
│  │  POST /chat/voice       - 语音输入（TTS/STT）│  │
│  │  GET  /chat/sessions    - 获取会话列表        │  │
│  └────────────────────┬───────────────────────┘  │
│                       │                           │
│  ┌────────────────────┴───────────────────────┐  │
│  │        evaluation_service.py (业务层)        │  │
│  │  - 评分数据聚合                               │  │
│  │  - AI对话上下文构建                           │  │
│  │  - 个性化建议生成                             │  │
│  └────────────────────┬───────────────────────┘  │
│                       │                           │
│  ┌────────────────────┴───────────────────────┐  │
│  │              ai_client.py                    │  │
│  │  - OpenAI/Claude API调用                     │  │
│  │  - 流式响应处理                               │  │
│  │  - Prompt模板管理                             │  │
│  └───────────────────────────────────────────┘  │
│                                                  │
│  ┌───────────────────────────────────────────┐  │
│  │              db.py (数据层)                 │  │
│  │  - chat_sessions 会话表                     │  │
│  │  - chat_messages 消息表                     │  │
│  └───────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
```

### 2.2 数据库新增表

```sql
-- AI对话会话表
CREATE TABLE chat_sessions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    title VARCHAR(200) DEFAULT '新的对话',
    context JSON,  -- 包含当前评测数据、用户画像等上下文
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

-- AI对话消息表
CREATE TABLE chat_messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id INTEGER NOT NULL,
    role VARCHAR(20) NOT NULL,  -- 'user' / 'assistant' / 'system'
    content TEXT NOT NULL,
    metadata JSON,  -- 包含引用的评分数据、建议ID等
    tokens_used INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (session_id) REFERENCES chat_sessions(id) ON DELETE CASCADE
);

-- 用户职业建议表（AI生成的长期建议）
CREATE TABLE career_advice (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    dimension VARCHAR(50),  -- 关联的评分维度
    advice_type VARCHAR(30),  -- 'immediate' / 'short_term' / 'long_term'
    content TEXT NOT NULL,
    priority INTEGER DEFAULT 0,
    is_read BOOLEAN DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);
```

### 2.3 后端API端点设计

#### 2.3.1 对话核心API

| 方法 | 端点 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/scoring/chat` | 发送消息，获取AI回复（支持SSE流式） | 需要登录 |
| GET | `/api/scoring/chat/history` | 获取当前会话的对话历史 | 需要登录 |
| GET | `/api/scoring/chat/sessions` | 获取用户的会话列表 | 需要登录 |
| POST | `/api/scoring/chat/session` | 创建新会话 | 需要登录 |
| DELETE | `/api/scoring/chat/session/<id>` | 删除会话 | 需要登录 |
| GET | `/api/scoring/chat/suggestions` | 获取AI预设问题建议 | 需要登录 |

#### 2.3.2 AI辅助API

| 方法 | 端点 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/scoring/ai/explain` | AI解释评分结果 | 需要登录 |
| POST | `/api/scoring/ai/roadmap` | AI生成提升路线图 | 需要登录 |
| POST | `/api/scoring/ai/compare` | AI对比分析（与他人/行业） | 需要登录 |

### 2.4 前端页面设计

#### 2.4.1 AI对话页面 (`ChatView.vue`)

```
┌─────────────────────────────────────────────────┐
│  导航栏                                         │
├──────────┬──────────────────────────────────────┤
│ 会话列表  │           对话区域                    │
│          │                                      │
│ ┌──────┐ │  ┌────────────────────────────────┐  │
│ │会话1  │ │  │ 🤖 你好！我是你的AI职业辅导     │  │
│ │会话2  │ │  │ 顾问。我看到你最近的评测结果...   │  │
│ │会话3  │ │  └────────────────────────────────┘  │
│ │ ...  │ │                                      │
│ └──────┘ │  ┌────────────────────────────────┐  │
│          │  │ 👤 我想了解如何提升沟通能力       │  │
│ [+新建]  │  └────────────────────────────────┘  │
│          │                                      │
│          │  ┌────────────────────────────────┐  │
│          │  │ 🤖 根据你的评测结果，你在沟通    │  │
│          │  │ 能力维度得分65分...              │  │
│          │  │                                │  │
│          │  │ 建议你从以下几个方面提升：       │  │
│          │  │ 1. 参加演讲俱乐部               │  │
│          │  │ 2. 主动承担项目汇报任务          │  │
│          │  │ 3. 学习非暴力沟通技巧            │  │
│          │  └────────────────────────────────┘  │
│          │                                      │
│          │  ┌──────────────────────────────┐    │
│          │  │ 输入消息...          [发送]    │    │
│          │  └──────────────────────────────┘    │
│          │  💡 建议问题: [如何提升领导力?]       │
│          │          [面试技巧有哪些?]             │
└──────────┴──────────────────────────────────────┘
```

#### 2.4.2 职业建议页面 (`AdviceView.vue`)

```
┌─────────────────────────────────────────────────┐
│  导航栏                                         │
├─────────────────────────────────────────────────┤
│  📋 我的职业建议                                 │
│                                                  │
│  ┌─────────────────────────────────────────┐    │
│  │ 🔴 紧急建议                              │    │
│  │ • 沟通能力需要立即关注，建议本周内...      │    │
│  │ • 时间管理得分偏低，推荐使用番茄工作法...  │    │
│  └─────────────────────────────────────────┘    │
│                                                  │
│  ┌─────────────────────────────────────────┐    │
│  │ 🟡 短期建议（1-3个月）                    │    │
│  │ • 参加学校的演讲比赛                      │    │
│  │ • 完成Coursera上的领导力课程              │    │
│  │ • 每周进行一次模拟面试                    │    │
│  └─────────────────────────────────────────┘    │
│                                                  │
│  ┌─────────────────────────────────────────┐    │
│  │ 🟢 长期建议（6-12个月）                   │    │
│  │ • 建立个人品牌，开始写技术博客             │    │
│  │ • 参与开源项目，积累实战经验               │    │
│  │ • 考取PMP项目管理认证                     │    │
│  └─────────────────────────────────────────┘    │
│                                                  │
│  [💬 与AI讨论这些建议]                          │
└─────────────────────────────────────────────────┘
```

### 2.5 AI对话Prompt设计

```python
# 系统提示词模板
SYSTEM_PROMPT = """你是一位专业的大学生职业发展AI顾问，名叫"启航导师"。

## 你的角色
- 帮助大学生进行职业规划和能力提升
- 基于学生的评测数据提供个性化建议
- 用鼓励和引导的方式与学生交流

## 你的能力
1. 分析学生的七维能力评分（专业知识、实践能力、职业素养、沟通能力、团队协作、创新思维、学习能力）
2. 根据评分结果生成个性化的提升建议
3. 提供具体的学习资源和行动方案
4. 回答职业规划相关问题

## 回复规范
- 语言亲切友好，像一位经验丰富的学长/学姐
- 回复要有条理，使用编号或分点
- 结合学生的具体数据说话，不要泛泛而谈
- 每次回复包含1-2个具体的行动建议
- 适当引用评测数据增强说服力

## 当前学生的评测数据
{student_scores}

## 当前对话上下文
{chat_context}
"""

# 评分解释提示词
EXPLAIN_PROMPT = """请详细解释以下评测结果：

维度：{dimension}
得分：{score}/100
得分等级：{level}

请从以下几个方面进行解释：
1. 这个分数意味着什么（与同龄人对比）
2. 得分的主要影响因素
3. 这个维度对职业发展的重要性
4. 具体的提升建议（至少3条）
"""
```

### 2.6 实现步骤

#### Phase 1: 后端基础（2天）

| 任务 | 优先级 | 工作量 |
|------|--------|--------|
| 创建chat_sessions和chat_messages数据库表 | P0 | 0.5天 |
| 实现chat_sessions CRUD（db.py） | P0 | 0.5天 |
| 实现chat_messages CRUD（db.py） | P0 | 0.5天 |
| 实现基础对话API（POST /chat） | P0 | 0.5天 |

#### Phase 2: AI集成（2天）

| 任务 | 优先级 | 工作量 |
|------|--------|--------|
| 集成OpenAI/Claude API（ai_client.py） | P0 | 1天 |
| 实现SSE流式响应 | P0 | 0.5天 |
| 构建评测数据上下文 | P0 | 0.5天 |

#### Phase 3: 前端对话（2天）

| 任务 | 优先级 | 工作量 |
|------|--------|--------|
| ChatView.vue对话界面 | P0 | 1天 |
| 会话列表侧边栏 | P1 | 0.5天 |
| SSE流式渲染 | P0 | 0.5天 |

#### Phase 4: 智能建议（1.5天）

| 任务 | 优先级 | 工作量 |
|------|--------|--------|
| AI建议生成API | P1 | 0.5天 |
| AdviceView.vue建议页面 | P1 | 0.5天 |
| 建议与对话联动 | P1 | 0.5天 |

#### Phase 5: 语音功能（1.5天）

| 任务 | 优先级 | 工作量 |
|------|--------|--------|
| 语音输入API（STT） | P2 | 0.5天 |
| 语音输出API（TTS） | P2 | 0.5天 |
| 前端语音交互组件 | P2 | 0.5天 |

**总计：约9天（1.8周）**

### 2.7 技术依赖

```txt
# 后端新增依赖
openai>=1.0.0          # OpenAI API
anthropic>=0.25.0      # Claude API（备选）
elevenlabs>=0.2.0      # TTS语音合成
speechrecognition>=3.10 # STT语音识别

# 前端新增依赖
marked>=12.0.0         # Markdown渲染
highlight.js>=11.9.0   # 代码高亮
```

### 2.8 环境变量配置

```bash
# .env 新增
AI_PROVIDER=openai          # openai / claude
OPENAI_API_KEY=sk-xxx
OPENAI_MODEL=gpt-4
CLAUDE_API_KEY=sk-ant-xxx
ELEVENLABS_API_KEY=xxx
TTS_VOICE_ID=21m00Tcm4TlvDq8ikWAM
```

---

## 三、实施优先级排序

### 第一优先级（必须完成）

1. **AI对话核心功能**
   - 后端对话API + 数据库表
   - OpenAI/Claude集成
   - 前端ChatView对话界面
   - SSE流式响应

2. **评测数据联动**
   - 对话时自动加载用户评测数据
   - AI基于评测结果给出建议
   - 对话中引用具体评分

### 第二优先级（应该完成）

3. **会话管理**
   - 多会话支持
   - 会话历史保存
   - 会话删除/新建

4. **智能建议系统**
   - AI生成个性化建议
   - 建议页面展示
   - 建议与对话联动

### 第三优先级（可以延后）

5. **语音交互**
   - 语音输入（STT）
   - 语音输出（TTS）
   - 语音对话模式

6. **高级功能**
   - AI对比分析
   - 提升路线图生成
   - 多轮对话上下文优化

---

## 四、风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| API调用成本高 | 中 | 实现token限制 + 缓存常见问答 |
| 流式响应不稳定 | 中 | 实现重试机制 + 降级为非流式 |
| AI回复质量不稳定 | 低 | 优化Prompt + 添加回复审核 |
| 语音识别准确率 | 低 | 支持文本输入作为备选 |

---

## 五、验收标准

### 功能验收

- [ ] 用户可以创建新对话
- [ ] 用户可以发送消息并获得AI回复
- [ ] AI回复基于用户的评测数据
- [ ] 对话历史可以保存和查看
- [ ] 支持流式输出（逐字显示）
- [ ] 可以删除不需要的对话

### 性能验收

- [ ] 首次回复时间 < 3秒
- [ ] 流式输出延迟 < 500ms
- [ ] 支持同时100个并发对话

### 体验验收

- [ ] 对话界面美观易用
- [ ] AI回复有条理、有温度
- [ ] 建议具体可执行
